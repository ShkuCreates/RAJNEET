# RAJNEET Fixes - TODO

## Step 1 (Approved)
- [x] Fix 3: Replace mock-only `components/polls/PollWidget.tsx` with real fetch from `/api/polls/featured`, add 3s timeout, empty/error states, and voting via `/api/polls/vote`.


## Step 2 (Approved)
- [x] Fix 4: Update `app/page.tsx` hero CTA copy/subtext logic for logged-in vs logged-out users.
- [x] Fix 4: Add frosted/overlay treatment on the landing mock breaking-news card for logged-out visitors.


## Step 3 (Not approved yet)
- [ ] Fix 1: Auth/Google OAuth/Render env verification (config-only; no code edits planned).
- [ ] Fix 2: Automation + Supabase realtime/RLS policy + feed behavior (repo currently lacks supabase realtime code; requires confirmation before code changes).

